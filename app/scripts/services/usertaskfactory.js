'use strict';

/**
 * @ngdoc service
 * @name rollApp.userTaskFactory
 * @description
 * # userTaskFactory
 * Service in the rollApp.
 */
angular.module('rollApp')
  .service('userTaskFactory', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
